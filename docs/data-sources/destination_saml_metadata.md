---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "custom_destination_saml_metadata Data Source - SAP BTP Custom"
subcategory: ""
description: |-
  Get the SAML IdP metadata used to share configuration information between the Identity Provider (IdP) and the Service Provider (SP). Note that it's generic (contains only the certificate) and not configured for a specific scenario (i.e. CF to CF, CF to Neo etc.) which requires additional properties.
  Further documentation:
  https://help.sap.com/docs/connectivity/sap-btp-connectivity-cf/set-up-trust-between-systems
---

# custom_destination_saml_metadata (Data Source)

Get the SAML IdP metadata used to share configuration information between the Identity Provider (IdP) and the Service Provider (SP). Note that it's generic (contains only the certificate) and not configured for a specific scenario (i.e. CF to CF, CF to Neo etc.) which requires additional properties.

**Further documentation:**
<https://help.sap.com/docs/connectivity/sap-btp-connectivity-cf/set-up-trust-between-systems>

## Example Usage

```terraform
# read the destination SAML IdP metadata
data "custom_destination_saml_metadata" "main" {
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Read-Only

- `idp_metadata` (String) Base64-encoded SAML IdP metadata XML
