package btp

// IdpMetadata response when reading the identity provider (IdP) metadata from the SAP Cloud Foundry Destination Service
type IdpMetadata struct {
	IdpMetadata string `json:"idpMetadata"`
}
